
This is the Perl distribution DBIx-Class-InflateColumn-Markup-Unified.

Installing DBIx-Class-InflateColumn-Markup-Unified is straightforward.

## Installation with cpanm

If you have cpanm, you only need one line:

    % cpanm DBIx::Class::InflateColumn::Markup::Unified

If you are installing into a system-wide directory, you may need to pass the
"-S" flag to cpanm, which uses sudo to install the module:

    % cpanm -S DBIx::Class::InflateColumn::Markup::Unified

## Installing with the CPAN shell

Alternatively, if your CPAN shell is set up, you should just be able to do:

    % cpan DBIx::Class::InflateColumn::Markup::Unified

## Manual installation

As a last resort, you can manually install it. Download the tarball, untar it,
then build it:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

If you are installing into a system-wide directory, you may need to run:

    % sudo make install

## Documentation

DBIx-Class-InflateColumn-Markup-Unified documentation is available as POD.
You can run perldoc from a shell to read the documentation:

    % perldoc DBIx::Class::InflateColumn::Markup::Unified
